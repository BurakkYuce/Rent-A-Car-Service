# F12 — Platform konsolu

> **Durum:** kod ✔ — F12.1 uçlar #281, F12.2 ekranlar #293, F12.3 parite (`F12-parite.md`; canlı parite YOK — kullanıcı kararı 2026-09-26) + kesiş (yönlendirme haritası, pilotsuz); Blazor sayfaları ve POST uçlarının silinmesi F13.1a · **PR:** 3 · **Para:** yok · **Önkoşul:** önceki fazın Exit'i

**Kapsam:** giriş, kiracılar, detay, belgeler + `/api/ui/v1/platform/*` (`PlatformIsolation` muafiyeti).

Platform oturumu tenant oturumundan ayrıdır; `platform_admin` claim'i ve `PlatformLayout` burada söner.

## Kalıp (F5–F12 ortak)

1. **Backend:** fazın **kendi seçim ve typeahead uçları** + ekran uçları + sayfalama/sıralama. Alt kayıt uçları
   üst kaydın kapsamından geçer. Aşağıdaki "Servis ihtiyacı" listesinde "bu fazda ilk kez" olan her servis bu
   fazda `/api/ui/v1` ucuna kavuşur; "önceki fazda açılmış" olanlar yeniden kullanılır.
2. **Ekranlar:** aşağıdaki sayfa listesine göre.
3. **Doğrulama:** parite + e2e.
4. **Kesiş (tek PR):**
   - Yönlendirme şablonları eklenir; menü kaydında sahip `spa` olur; Blazor sayfaları silinir.
   - Hedefsiz Blazor POST uçları silinir (aşağıdaki tabloda "Silineceği kesiş" bu faz olanlar). Kanıt: uç rotası
     tüm razor'da aranır, `@(…)` ve `formaction` dahil. Hâlâ kullanılanlar sonraki faza ya da F13'e kalır.
   - Fazın guard testleri devredilir.

## Exit

- Parite %100 · e2e yeşil.
- Aşağıdaki listedeki `@page` = 0.
- Yönlendirme negatif testi yeşil (PDF/export/hesap uçları yönlenmez).
- Devredilen testler PR'da listeli.

## Envanter

<!-- ENVANTER:BASLA — scripts/roadmap-envanteri.py üretir; elle düzenlemeyin -->

**Sayfa:** 4 dosya · 4 `@page` rotası · 481 satır

| Dosya (`Components/Pages/`) | Rota | Satır | Servisler | POST hedefi |
|---|---|---|---|---|
| `Layout/PlatformLayout.razor` | `(kabuk)` | 25 | — | 1 |
| `Platform/Belgeler.razor` | `/platform/belgeler` | 136 | PlatformAdminService | 4 |
| `Platform/PlatformLogin.razor` | `/platform/login` | 41 | — | 1 |
| `Platform/TenantConsole.razor` | `/platform/tenants` | 73 | PlatformAdminService | 2 |
| `Platform/TenantDetail.razor` | `/platform/tenants/{Id:guid}` | 206 | PlatformAdminService | 7 |

**Servis ihtiyacı** (her servis, onu ilk kullanan fazda `/api/ui/v1` ucuna kavuşur):

- Bu fazda ilk kez (1): `PlatformAdminService`
- Önceki fazda açılmış (0): —

**Blazor POST uçları** (14). Bir uç, onu kullanan son fazın kesişinde silinir:

| Uç | Kullanan fazlar | Silineceği kesiş |
|---|---|---|
| `/platform/auth/login` | F12 | F12 (bu faz) |
| `/platform/auth/logout` | F12 | F12 (bu faz) |
| `/platform/belgeler/yukle` | F12 | F12 (bu faz) |
| `/platform/belgeler/{id}/durum` | F12 | F12 (bu faz) |
| `/platform/belgeler/{id}/sil` | F12 | F12 (bu faz) |
| `/platform/belgeler/{id}/surum` | F12 | F12 (bu faz) |
| `/platform/tenants/close` | F12 | F12 (bu faz) |
| `/platform/tenants/create` | F12 | F12 (bu faz) |
| `/platform/tenants/logo` | F12 | F12 (bu faz) |
| `/platform/tenants/logo-sil` | F12 | F12 (bu faz) |
| `/platform/tenants/modul-web-sitesi` | F12 | F12 (bu faz) |
| `/platform/tenants/reopen` | F12 | F12 (bu faz) |
| `/platform/tenants/toggle` | F12 | F12 (bu faz) |
| `/platform/tenants/update` | F12 | F12 (bu faz) |

<!-- ENVANTER:BITIR -->
