# F0 — Hazırlık

> **Durum:** sürüyor (F0.1) · **PR:** 2 · **Para:** yok · **Önkoşul:** G0 ✔ (Revlo graphify grafiği, 2026-09-21)

Geçişin başlangıç koşulları: belgeler tek yerde, faz listeleri mekanik, canlı kullanıcıyı yanıltan Blazor
hataları kapalı. Bu fazdan sonra Blazor'a yeni özellik girmez (bkz. [README](README.md) "Kilitli kararlar").

## PR'lar

1. **F0.1 Belgeler**
   - `KARARLAR.md` ve `YAPILMAZ.md` → [`docs/KARARLAR.md`](../KARARLAR.md) + "Açık işler" bölümü.
   - Koddaki ve belgelerdeki atıflar güncellenir; bayat atıflar düzeltilir (parite FAZ-71).
   - Eski `docs/roadmap/*` (tamamlanmış 68 fazlık roadmap) silinir; git geçmişinde kalır.
   - Yeni `F*.md` dosyaları: sayfa listesi, servis ihtiyacı ve Blazor POST uçları
     [`scripts/roadmap-envanteri.py`](../../scripts/roadmap-envanteri.py) ile **mekanik** üretilir.
     [`DEGISIKLIKLER.md`](DEGISIKLIKLER.md) açılır.
   - `MIMARI.md` roadmap atıfları; CLAUDE.md §2 ("Blazor dondurulmuş, geçiş sürüyor"), §6 ve §9.
2. **F0.2 Blazor son kritik düzeltmeler** (canlıdaki UI denetimi, 2026-09-21)
   - Panel'de gecikmiş dönüş varsayılan sekme; gecikme sayısı görsel olarak öne çıkar.
   - `button[data-confirm]` desteği (bugün 6 geri alınamaz işlem onaysız geçiyor) + onaysız 24 yıkıcı
     form (sayı PR'da yeniden ölçülür).
   - Global gönderim kilidi (çift tıklamada ikinci POST yok).
   - `.num` sağa yaslama (sınıf 978 yerde kullanılıyor, CSS'te tanımı yok).
   - Giriş sonrası Panel + güvenli `ReturnUrl` (yerel URL doğrulamalı).
   - Türkçe 404/500 sayfaları.

## Exit

- `git grep -E 'docs/roadmap/(KARARLAR|SIRA|FAZ-|YAPILMAZ)'` = 0.
- `python3 scripts/roadmap-envanteri.py --kontrol` temiz.
- F0.2 canlıda doğrulandı (duman testi: gecikme sekmesi, onay diyaloğu, çift tıklama, ReturnUrl, 404/500).
- Backend test tabanı kaydedildi (test sayısı + "Failed: 0").
