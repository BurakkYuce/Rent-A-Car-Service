# F2 — Frontend projesi iskeleti

> **Durum:** bekliyor · **PR:** 2 · **Para:** yok · **Önkoşul:** F0 Exit. **F2.1, F1 ile paralel yürür**
> (iskelet, lint ve CI hiçbir backend ucuna bağlı değil); F2.2, F1 Exit'ini bekler.

## PR'lar

1. **F2.1 İskelet ve kapılar**
   - `src/RentACar.Frontend`: Angular 21.2 (sabit sürüm), `baseHref: "/app/"`, strict + `strictTemplates`,
     zoneless, yol takma adları.
   - ESLint 9 flat config + typescript-eslint + angular-eslint. Kurallar: katman sınırları, OnPush, `any` yasak,
     ham `toLowerCase` yasak (Türkçe İ/ı), mutlak URL yasak (XSRF header'ı yalnız göreli URL'de eklenir).
   - Prettier + lint-staged, Vitest, Playwright + axe.
   - `inlineCritical: false`; gerçek bütçeler.
   - CI `frontend` işi: lint, tip, birim, build.
   - `AGENTS.md`.
   - Kök `.graphifyignore`'a `*.html`, `*.scss`, `*.json`, `test-results/`, `playwright-report/` ve üretilmiş
     tipler eklenir (aksi halde HTML doküman sayılır, tam koşuda token harcar).
2. **F2.2 Tipler, e2e, geliştirme akışı ve dağıtım**
   - `docs/api/ui-v1.json` → `openapi-typescript` (prebuild). Kayma derleme hatası olur.
   - Geliştirme akışı: `npm run watch` → `Spa:Dizin`, uygulama `:5220`'de.
   - CI `e2e` işi.
   - **CI artifact:** `main`'e her push'ta SPA derlenir. Çıktı `spa-<sha>.tar.gz` + `sha256` + hash'li dosya
     listesi (`chunks.txt`); GitHub release asset olarak yüklenir.
   - `yayinla.sh`:
     - checkout edilen SHA'nın artifact'ını salt-okur token ile (`/etc/racar/racar.env`) indirir;
     - checksum doğrular, `releases/<ts>/app/`'e açar;
     - önceki release'in **kendi** `chunks.txt`'indeki dosyaları `cp -n` ile **`chown`'dan önce** kopyalar
       (açık sekmeler eski chunk'ları bulur; sınırlı, birikmez);
     - aynı symlink swap ve geri alma.
   - `kurulum.sh`: SDK kontrolü `dotnet --list-sdks | grep -q '^10\.'`. Sunucuda Node yok.
   - `deploy-checklist` güncellenir.

## Exit

- Production'da `/app/` 200 dönüyor; veri istekleri 401/`pilot_degil`.
- Her CI kapısının bozukken kırmızı olduğu kanıtlandı.
- Geri alma provası yapıldı.
- Yanlış SHA ya da checksum ile yayın reddediliyor.
