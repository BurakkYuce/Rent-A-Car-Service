# F3 — Çekirdek

> **Durum:** ✔ tamamlandı (2026-09-22) · **PR:** 7 (F3.3 ve F3.5 küçük backend eki içerir) · **Para:** yok · **Önkoşul:** F1 + F2 Exit

Tasarım sistemi, kabuk, oturum, veri katmanı, tablo, form seti. Modül fazları (F4–F12) yalnız bu çekirdeğin
üstüne ekran yazar; çekirdekte eksik çıkarsa o faza "çekirdek eki" PR'ı olarak girer, sıra değişmez.

## Revlo'dan taşıma haritası

Kaynak: `~/Desktop/revlo-market-ai/revlo-angular` (kod DEĞİŞMEZ). Kapanış 265 dosya / 58,5 bin satır; ~45 bini
elle yazılmış. Taşıma **yapraktan köke** yapılır. Revlo'da 9 iç import döngüyü kuruyor: `locale.service.ts:25`,
`auth.service.ts:22`, `toast.component.ts:15`, `date-picker.component.ts:40-41`, `wizard-layout.component.ts:20`,
`table-i18n.utils.ts:6`, `list-query-dynamic-filter.component.ts:15`, `overlay-manager.service.ts:3`,
`theme.service.ts:14`. Bunlar kesilip ince `DilServisi` / `OturumServisi` arayüzlerine bağlanır.

Her parça zoneless'a uyarlanır, spec'ler Vitest'e çevrilir, derlenmeyen spec taşınmaz. Bağ soruları önce G0
grafiğine sorulur: `graphify query "<soru>" --graph ~/Desktop/revlo-market-ai/revlo-angular/graphify-out/graph.json`
(spec'ler grafta yok → spec kararları `grep` ile).

| Parça | Revlo kapanışı | Yöntem | Zorluk | PR |
|---|---|---|---|---|
| Token + global stiller | 9 / 2,3 bin | Kopyala; `--revlo-*` → `--rc-*` | S | F3.1 |
| Primitifler + ikon üreticisi | 67 / 16,8 bin | Kopyala; `source-icon` atılır | M | F3.1 |
| i18n | 11 / 6,2 bin | Kopyala; anahtarlar yeniden üretilir | M | F3.1 |
| Oturum / izin / hata | 18 / 2,9 bin | **Yeniden yaz**; `withCredentials` ve `core/errors` alınır | M | F3.3 |
| Toast, modal/çekmece/sihirbaz | 16 / 10,8 bin · 30 / 11,8 bin | Kopyala; CDK odak kilidi + Esc | M | F3.3 |
| FetchPolicy | 5 / 1,4 bin | Kopyala; HotelService → oturum bağlamı | S | F3.2 |
| Sekmeli çalışma alanı | 30 / 11,4 bin | Kopyala + 11 bağı kes; localStorage'da yalnız rota + id (KVKK) | L | F3.2 |
| Kabuk | 92 / 21,9 bin | **Yeniden yaz**; bottom-nav, nav-item alınır | L | F3.2 |
| Liste sorgusu + filtreler | 84 / 24,2 bin | Kopyala; tipli katalog | M | F3.4 |
| Tablo motoru | 155 / 38,2 bin | Kopyala; `TabloDuzenleri`; `@tanstack/table-core` ve `virtual-core` açık | M | F3.5 |
| Tarih seçici | 34 / 16,2 bin | Kopyala; döngü kenarları kesilir | M | F3.6 |

## Kabuk envanteri

<!-- ENVANTER:BASLA — scripts/roadmap-envanteri.py üretir; elle düzenlemeyin -->

**Kabuğun veri ihtiyacı** (Blazor `MainLayout`'un `@inject` satırlarından). Bu servisler yeni kabukta F3.2'de gerekir; uçları burada açılır ve sonraki fazlarda "önceki fazda açılmış (F3)" olarak görünür:

- `Layout/MainLayout.razor` (Blazor'da F13'e kadar yaşar): `BildirimService`, `ITenantContext`, `ITenantSettingsRepository`, `PublicBookingRequestService`, `TenantStatusCache`

<!-- ENVANTER:BITIR -->

## PR'lar

1. **F3.1 Tasarım sistemi:** ERP token'ları (yoğun; açık ve koyu tema; sabit tip ölçeği), kiracı renkleri,
   ikonlar, self-host fontlar, `LOCALE_ID='tr'` ve pipe'lar, tr normalize, i18n (yalnız `tr.json`).
2. **F3.2 Kabuk**
   - Menü kayıttan (grup, sıra, modül, rozet, hızlı bağlantı). Blazor öğeleri tam sayfa açılır; kaydedilmemiş
     değişiklik koruması devrede.
   - Aktif sayfa işareti, topbar, mobil çekmece, sekmeler + FetchPolicy, Ctrl+K.
3. **F3.3 Oturum ve geri bildirim**
   - Giriş sayfası, oturum servisi, izin guard'ı.
   - Interceptor, `kod`'a göre:
     - `dogrulama` → alan hatası;
     - `oturum_yok` → **yerinde yeniden giriş diyaloğu** + istek tekrarı (form kaybolmaz);
     - `kiraci_kapali` → mesajlı giriş sayfası;
     - `yetki_yok` / `pilot_degil` → uyarı bandı;
     - `cakisma` → alan hatası ya da bant, **form korunur**;
     - `mukerrer` → kayıt yeniden yüklenir + bilgi;
     - 5xx → toast.
   - Toast, CDK onay diyaloğu. Çıkışta tam temizlik (store'lar, sorgu önbelleği, sekmeler).
   - `/api/ui/v1/istemci-hata` (backend eki).
   - `bilgi` / `hata` sorgu parametrelerinin gösterimi.
   - Yeni sürüm algılanınca "yenile" bildirimi; ChunkLoadError'da kontrollü yenileme.
4. **F3.4 Veri katmanı:** `TemelStore<T>` (lint ile zorunlu; dört durum, `switchMap` ile önceki isteği iptal,
   hata asla boş liste olarak gösterilmez), tipli liste sorgusu + URL senkronu, katlanır filtre.
5. **F3.5 Tablo motoru**
   - Yoğun satır, sabit ilk sütun ve başlık, para sağa yaslı, tr biçim, klavye gezinmesi.
   - `TabloDuzenleri` (backend eki, RLS'li).
   - Dışa aktarma sunucu uçlarıyla.
6. **F3.6 Form seti**
   - CVA kontroller: para ve CDK listbox arama-seçim dahil.
   - Alan hata bileşeni, kaydedilmemiş değişiklik koruması, gönderim kilidi + anahtar yenileme kuralı.
   - Tam sayfa sekmeli form + sabit yan panel.
   - Genel tanım CRUD bileşeni (F11'in 24 tanım ekranı bununla), print CSS.
7. **F3.7 Vitrin ve kontroller:** görsel regresyon (açık/koyu × 320/390/768/1440), axe, mobil taşma; hepsi CI'da.

## Exit

- Vitrin eksiksiz · axe ciddi/kritik 0 · birim testler yeşil.
- e2e yeşil: "doğrulama hatasında form korunur", "oturum düşünce form kaybolmaz", "`cakisma` formu silmez".

## Kapanış kaydı (2026-09-22)

| PR | # | Not |
|---|---|---|
| F3.1 Tasarım sistemi | #250 | `--rc-*` token'ları, yoğun yerleşim, açık/koyu tema (`TemaServisi`), tr pipe'ları, Transloco `tr.json`, Tabler ikonları |
| F3.2 Kabuk | #254 | menü `/api/ui/v1/menu`'den, `aria-current`, Ctrl+K paleti, mobil çekmece, sekmeli çalışma alanı (localStorage'da yalnız rota + id — KVKK) |
| F3.3 Oturum ve geri bildirim | #253 | `OturumServisi`, guard'lar, `kod` bazlı interceptor, yerinde yeniden giriş, toast/bant/CDK onay, `/api/ui/v1/istemci-hata` (backend eki) |
| F3.4 Veri katmanı | #248 | `api-istemcisi`, tipli `ApiHatasi`, 4 durumlu `TemelStore` (`switchMap`), liste sorgusu + URL senkronu, FetchPolicy |
| F3.5 Tablo motoru | #252 | TanStack `table-core`/`virtual-core`; `TabloDuzenleri` (backend eki, RLS'li) |
| F3.6 Form seti | #251 | CVA kontroller (para/tarih), alan hatası, kaydedilmemiş değişiklik koruması, gönderim kilidi + anahtar yenileme, sekmeli form, tanım CRUD |
| F3.7 Vitrin ve kontroller | #255 | vitrin dizini; axe ciddi/kritik 0 (açık + koyu); 320/390/768/1440 gövde taşması 0; 72 ekranlık görsel regresyon (tabanlar CI imajıyla aynı Docker imajında üretildi); ilk paket 381 → 372 kB, bütçe uyarı 380 / hata 450 kB |

Exit: vitrin eksiksiz · axe ciddi/kritik 0 · Vitest 453/453 · e2e yeşil ("doğrulama hatasında form korunur", "oturum düşünce form kaybolmaz", "`cakisma` formu silmez" — `form.spec.ts` + `oturum.spec.ts`). Faz içi PR'lar paralel geliştirildi; merge sırası 248, 250, 251, 252, 253, 254, 255.

**Önkoşul notu:** F3, F2 Exit'inin sunucu maddeleri (production `/app/` doğrulaması, geri alma provası, yanlış SHA reddi) beklenmeden başladı ve bitti; bkz. [DEGISIKLIKLER](DEGISIKLIKLER.md). F2.2'nin kod tarafı (#249) merge edildi, sunucu adımları `docs/ops/f2-2-sunucu-adimlari.md`'de kullanıcıyı bekliyor.
