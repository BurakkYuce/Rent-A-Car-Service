# F13 — Blazor söküm ve kapanış

> **Durum:** bekliyor · **PR:** 2 · **Para:** yok · **Önkoşul:** F12 Exit

## PR'lar

1. **F13.1 Söküm**
   - Silinecekler: `Components/Pages`, `Layout`, `wwwroot/js|css`, Radzen, flatpickr, kalan Blazor POST uçları
     (aşağıdaki tablo + önceki fazlardan F13'e kalanlar).
   - `LoginPath`, `AccessDeniedPath`, `/Error`, `/not-found`, `/hata` SPA'ya bağlanır.
   - 301'ler kalıcı olur.
   - Pilot kolonu ve kapısı kaldırılır.
   - `mobil-tasma` ve kök `package.json` kaldırılır.
2. **F13.2 Belgeler:** CLAUDE.md §2 (yığın: Angular), `MIMARI.md`, roadmap kapanışı.

## Exit

- `@page` = 0.
- Web'de `MapPost` yalnız `/api/ui` ve oturum uçlarında.
- CI yeşil · canlı duman testi geçti.

## Envanter

<!-- ENVANTER:BASLA — scripts/roadmap-envanteri.py üretir; elle düzenlemeyin -->

**Sayfa:** 4 dosya · 4 `@page` rotası · 521 satır

| Dosya (`Components/Pages/`) | Rota | Satır | Servisler | POST hedefi |
|---|---|---|---|---|
| `DogrulamaHatasi.razor` | `/hata` | 31 | — | 0 |
| `Error.razor` | `/Error` | 36 | — | 0 |
| `Layout/MainLayout.razor` | `(kabuk)` | 424 | BildirimService, ITenantContext, ITenantSettingsRepository, PublicBookingRequestService, TenantStatusCache | 1 |
| `NotFound.razor` | `/not-found` | 5 | — | 0 |
| `Yetkisiz.razor` | `/yetkisiz` | 25 | — | 0 |

**Servis ihtiyacı** (her servis, onu ilk kullanan fazda `/api/ui/v1` ucuna kavuşur):

- Bu fazda ilk kez (0): —
- Önceki fazda açılmış (5): `InAppNotificationService` (F3), `ITenantContext` (F3), `ITenantSettingsRepository` (F3), `PublicBookingRequestService` (F3), `TenantStatusCache` (F3)

**Blazor POST uçları** (1). Bir uç, onu kullanan son fazın kesişinde silinir:

| Uç | Kullanan fazlar | Silineceği kesiş |
|---|---|---|
| `/auth/logout` | F13 | F13 (bu faz) |

<!-- ENVANTER:BITIR -->
