# RentACar — üretim ortam değişkenleri (systemd EnvironmentFile).
#
# Bu dosyanın sunucudaki yeri: /etc/racar/racar.env  (mod 0640, sahibi root:racar)
# ÜÇ servis de (web/api/publicsite) AYNI dosyayı okur — anahtarların ayrışması imkânsız olsun diye.
#
# !!! kurulum.sh bu dosyayı VARSA ASLA ÜZERİNE YAZMAZ. Sebebi ölümcül: Pii__HmacKey değişirse
# !!! kayıtlı TC blind-index'leri eşleşmez (TC araması sessizce boş döner), RACAR_DP_KEYS başka
# !!! bir dizini gösterirse şifreli PII (*Enc) KALICI olarak çözülemez.
# !!! Anahtarları elle değiştirmen gerekiyorsa önce yedek + geçiş planı; körlemesine döndürme.

ASPNETCORE_ENVIRONMENT=Production

# --- Veritabanı ---------------------------------------------------------------
# Default  = racar_app  (NOSUPERUSER NOBYPASSRLS) — uygulama DAİMA bununla bağlanır, RLS'in temeli.
# Migrator = racar_owner (DDL) — yalnız açılıştaki migration için.
ConnectionStrings__Default=Host=localhost;Port=5432;Database=racar;Username=racar_app;Password=DEGISTIR_APP_PW
ConnectionStrings__Migrator=Host=localhost;Port=5432;Database=racar;Username=racar_owner;Password=DEGISTIR_OWNER_PW

# --- Şifreleme anahtarları (YEDEKLE — kaybolursa veri kurtarılamaz) -----------
# Pii__HmacKey : TC blind-index (kısmi unique index + tam-eşleşme araması)
Pii__HmacKey=DEGISTIR_openssl_rand_base64_48
# RACAR_DP_KEYS: DataProtection key-ring KALICI dizini. Üç binary de AYNI dizin.
# /opt/racar ALTINDA OLMAMALI — deploy dizini değiştiğinde ring ile birlikte PII gider.
RACAR_DP_KEYS=/var/lib/racar/dp-keys

# --- Platform süper-admin konsolu (/platform) --------------------------------
Platform__AdminUser=DEGISTIR
# Düz şifre DEĞİL, hash (PlatformCredentials.HashPassword ile üretilir).
Platform__AdminPasswordHash=DEGISTIR

# --- Yalnız API (racar-api.service) ------------------------------------------
# API'yi yayınlamıyorsan boş bırakabilirsin; servis de kapalı kalsın.
Jwt__Key=DEGISTIR_openssl_rand_base64_48

# --- Opsiyonel: telemetri (SET DEĞİLSE hiç exporter kurulmaz) ----------------
# OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4317
# Observability__AlertToken=

# --- Opsiyonel: WhatsApp + SMS (Twilio) --------------------------------------
# Config yoksa gönderici DEVREYE GİRMEZ ve stub gönderim yapmadan false döner
# (sahte başarı yoktur — mesaj gitmediyse çağıran bunu görür).
# Twilio__AccountSid=
# Twilio__AuthToken=
# WhatsApp gönderen numarası (whatsapp: öneki KOD tarafından eklenir)
# Twilio__WhatsAppFrom=
# Kritik alarmların (OpsWatchdog) gideceği numara
# Twilio__AlertPhone=
#
# SMS için AYRI bir sağlayıcı gerekmez — aynı Twilio hesabı kullanılır. Yalnız bir
# gönderen kaynağı şarttır: numara VEYA Messaging Service (ikisi birlikte gönderilemez).
# Twilio__SmsFrom=+1XXXXXXXXXX
# Twilio__MessagingServiceSid=MGxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
#
# NOT: tenant kendi SMS başlığını (Ayarlar → SMS Başlığı) girerse gönderen olarak o kullanılır.
# Alfanümerik başlık Türkiye'de operatör kaydı ister; kayıtsız başlıkla mesaj teslim EDİLMEZ
# (Twilio 30007). Kayıt yoksa başlığı boş bırakıp numara ile gönderin.
#
# --- Ödeme (iyzico) ----------------------------------------------------------
# Config yoksa ödeme adaptörü DEVREYE GİRMEZ ve stub sahte işlem referansı üretmeden
# "yapılandırılmadı" döner. Kart verisi sunucumuza HİÇ uğramaz: müşteri iyzico'nun
# barındırılan ödeme sayfasında kartını girer (PCI kapsamı dışı).
# Iyzico__BaseUrl=https://sandbox-api.iyzipay.com   # ÜRETİM: https://api.iyzipay.com
# Iyzico__ApiKey=
# Iyzico__SecretKey=
#
# UYARI: üretimde "sandbox-" ile başlayan anahtar kullanmak = hiç para tahsil etmemek.
# Uygulama bunu açılışta gürültülü uyarır ama açılışı ENGELLEMEZ (staging bilinçli
# olarak sandbox kullanabilir). Canlıya geçerken BaseUrl'i de değiştirmeyi unutmayın.

# --- E-posta (SMTP) ----------------------------------------------------------
# SMTP ayarı ortam değişkeninde DEĞİL, her tenant'ın kendi Ayarlar ekranındadır
# (host/port/kullanıcı/şifre/gönderen adres). Şifre at-rest şifreli saklanır.
# Sunucu tarafında yapılacak bir şey yoktur; giden port 587/465'in açık olması yeterlidir.
