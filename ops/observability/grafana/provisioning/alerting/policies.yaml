apiVersion: 1

# Bildirim politikası: tüm alarmlar racar-webhook contact-point'ine (grupla + 5dk tekrar).
policies:
  - orgId: 1
    receiver: racar-webhook
    group_by: ["alertname"]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
